{
  "selects": [
    {
      "columns": [
        {
          "type": "String",
          "value": "SELECT _database, _table, n FROM merge(currentDatabase(), ^t) ORDER BY _database, _table, n"
        }
      ]
    }
  ]
}
