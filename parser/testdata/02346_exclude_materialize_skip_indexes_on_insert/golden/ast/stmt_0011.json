{
  "selects": [
    {
      "columns": [
        {
          "type": "String",
          "value": "idx_a is excluded, so it should perform no filtering, while `id,x_b` should perform filtering since it is included"
        }
      ]
    }
  ]
}
